name: Build FOX Live Translator APK

on:
  push:
    branches: [main]
    paths:
      - '02/**'
      - '.github/workflows/build-fox-translator.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Build debug APK
        working-directory: '02'
        run: gradle assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: FOX-Live-Translator-debug
          path: 02/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

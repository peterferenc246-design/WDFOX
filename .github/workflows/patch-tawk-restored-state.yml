name: One-time patch restored Tawk state

on:
  push:
    branches: [main]
    paths:
      - 'scripts/patch_tawk_restored_state.py'
      - '.github/workflows/patch-tawk-restored-state.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply patch
        run: python scripts/patch_tawk_restored_state.py
      - name: Commit loader fix
        run: |
          git config user.name "WDFOX Tawk Bot"
          git config user.email "actions@github.com"
          git add public/tawk-language-loader.js
          git commit -m "Handle restored Tawk chat state after refresh"
          git push origin main
